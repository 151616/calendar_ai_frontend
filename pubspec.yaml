name: calendar_ai_frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # ↓ Add packages here (NOT inside the flutter: block)
  speech_to_text: ^6.6.0
  flutter_tts: ^3.6.3
  http: ^1.2.0
  permission_handler: ^11.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
